import Root from './root.svelte';
import Image from './image.svelte';
import Fallback from './fallback.svelte';

export {
	Root,
	Image,
	Fallback,
	// Export components as Root, Image, Fallback
	Root as AvatarRoot,
	Image as AvatarImage,
	Fallback as AvatarFallback
};
